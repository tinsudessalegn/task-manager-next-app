export interface Todo {
    id: number,
    todo: string,
    completed: boolean,
    userId: number
}

export type ToDo = {
    id: number,
    todo: string,
    completed: boolean,
    userId: number
}